const searchContainer = {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
    width: '80%',
    marginTop: '100px'
}

const tile = {
    display: 'flex',
    flexDirection: 'column',
    alignContent: 'center', 
    alignItems:'center', 
    width: '25%',
    padding: '20px'
}

const wrapper = {
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
}

module.exports = {
    searchContainer: searchContainer,
    tile: tile,
    wrapper: wrapper
}